<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.commerceerp.system.mapper.TokenMapper">

    <insert id="saveToken">
        INSERT INTO token (id,token,type,date) VALUES (#{id},#{code},#{type},now())
    </insert>

</mapper>